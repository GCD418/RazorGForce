@*
*@
@{
}
<div class="modal fade" id="confirmSaveModal" tabindex="-1" aria-labelledby="confirmSaveModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content border-0 shadow">
            <div class="modal-header modal-header-blue text-white">
                <h5 class="modal-title" id="confirmSaveModalLabel">
                    <i class="fas fa-question-circle"></i> Confirmar guardado
                </h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                ¿Deseas guardar este registro?
                <p class="small text-muted mb-0">Verifica que los datos sean correctos antes de confirmar.</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">
                    <i class="fas fa-times"></i> Cancelar
                </button>
                <button type="button" id="confirmSaveBtn" class="btn btn-dark">
                    <i class="fas fa-check"></i> Sí, guardar
                </button>
            </div>
        </div>
    </div>
</div>

<style>
    .modal-header-blue {
        background-color: #202C45;
    }

    .modal-header-blue .modal-title {
       color: white; 
    }
    .modal-header-blue .modal-title i {
        color: white;
    }

        .modal-header-blue .btn-close {
            opacity: 1 !important;
        }

            .modal-header-blue .btn-close::before,
            .modal-header-blue .btn-close::after {
                background-color: white !important;
            }
</style>